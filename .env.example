# Production : PostgreSQL hébergé (Supabase, Neon, Railway, etc.)
# DATABASE_URL=postgresql://user:password@host:5432/recyclage

# Local / Docker sans variable : SQLite dans data/store/recyclage.db

# Connexion applicative (premier démarrage si table app_users vide)
# APP_DEFAULT_USER=admin
# APP_DEFAULT_PASSWORD=leadconnect
